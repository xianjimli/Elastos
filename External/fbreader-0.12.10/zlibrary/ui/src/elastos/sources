TARGET_NAME=ZLibrary
TARGET_TYPE=lib

ifeq "$(XDK_TARGET_PLATFORM)" "win32"
  C_DEFINES+=-UWIN32
  C_DEFINES+=-U_WIN32
endif

EXPORT_LIBRARY = 1

INCLUDES +=$(MAKEDIR)/../../../core/include

SOURCES += ZLibrary.cpp
