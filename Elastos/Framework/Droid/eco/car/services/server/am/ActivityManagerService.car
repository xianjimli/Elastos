
module
{
    interface IActivityManagerService {
        GetSystemContext(
            [out] IContext** ctx);

        SetSystemProcess();

        SetWindowManager(
            [in] IWindowManager* wm);
    }
}
