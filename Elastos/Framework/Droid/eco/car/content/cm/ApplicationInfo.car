
module
{
    interface IApplicationInfo {
        GetSourceDir(
            [out] String* sourceDir);

        SetSourceDir(
            [in] String sourceDir);

        GetTargetSdkVersion(
        	[out] Int32* targetSdkVersion);
    }
}
