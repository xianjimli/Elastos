
module
{
    interface IAccount {
        GetName(
            [out] String* name);

        SetName(
            [in] String name);

        GetType(
            [out] String* type);

        SetType(
            [in] String type);
    }
}
