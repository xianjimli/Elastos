
module
{
    interface IApartment;
    interface IMessage;

    interface ISslErrorHandler extends IApartment {

        /**
         * Proceed with the SSL certificate.
         */
        Proceed();

        /**
         * Cancel this request and all pending requests for the WebView that had
         * the error.
         */
        Cancel();
    }
}