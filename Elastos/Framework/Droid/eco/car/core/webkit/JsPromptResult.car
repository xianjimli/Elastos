
module
{
    interface IJsResult;

    interface IJsPromptResult extends IJsResult {

        /**
         * Handle a confirmation response from the user.
         */
        ConfirmPrompt(
           [in] CString result);
    }
}