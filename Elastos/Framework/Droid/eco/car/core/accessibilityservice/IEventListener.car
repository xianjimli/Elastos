
module
{
    interface IEventListener extends IBinder {
        SetConnection(
            [in] IAccessibilityServiceConnection* connection);

        OnAccessibilityEvent(
            [in] IAccessibilityEvent* event);

        OnInterrupt();
    }
}
