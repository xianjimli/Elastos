module
{
    interface ISQLiteCompiledSql
    {
        ReleaseSqlStatement();

        /**
         * returns true if acquire() succeeds. false otherwise.
         */
        Acquire(
            [out] Boolean* result);

    }
}