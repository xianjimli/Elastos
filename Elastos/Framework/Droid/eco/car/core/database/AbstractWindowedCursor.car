module
{
    interface ICursorWindow;

    interface IAbstractWindowedCursor extends ICrossProcessCursor {
        IsBlob(
            [in] Int32 columnIndex,
            [out] Boolean* result);

        IsString(
            [in] Int32 columnIndex,
            [out] Boolean* result);

        IsInt64(
            [in] Int32 columnIndex,
            [out] Boolean* result);

        IsFloat(
            [in] Int32 columnIndex,
            [out] Boolean* result);

        SetWindow(
            [in] ICursorWindow* window);

        HasWindow(
            [out] Boolean* result);
    }
}