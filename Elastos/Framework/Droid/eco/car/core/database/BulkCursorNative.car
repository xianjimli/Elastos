module
{
    interface ICursorWindow;
    interface IContentObserver;
    interface IBundle;

    interface IBulkCursorNative extends IBinder
    {
        AsInterface(
            [in] IBinder* obj,
            [out] IBulkCursor** bc);

        OnTransact(
            [in] Int32 code,
            [in] IParcel* data,
            [in] IParcel* reply,
            [in] Int32 flags,
            [out] Boolean* rst);

        AsBinder(
            [out] IBinder** b);
    }
}