
module
{
    interface IActivityManager;

    interface IPermissionController {
        CheckPermission(
            [in] CString permission,
            [in] Int32 pid,
            [in] Int32 uid,
            [out] Boolean* result);
    }
}
