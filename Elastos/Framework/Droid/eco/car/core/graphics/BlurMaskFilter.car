
module
{
    enum BLUR{
        BlurMaskFilter_BLUR_NORMAL = 0,  //!< blur inside and outside of the original border
        BlurMaskFilter_BLUR_SOLID = 1,   //!< include the original mask, blur outside
        BlurMaskFilter_BLUR_OUTER = 2,   //!< just blur outside the original border
        BlurMaskFilter_BLUR_INNER = 3,
    }

    interface IBlurMaskFilter extends IMaskFilter {
    }
}
