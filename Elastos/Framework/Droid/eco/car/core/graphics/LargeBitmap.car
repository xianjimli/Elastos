
module
{
    interface ILargeBitmap {
        DecodeRegion(
            [in] IRect* rect,
            [in] IBitmapFactoryOptions* options,
            [out] IBitmap** Bitmap);

        GetWidth(
            [out] Int32* width);

        GetHeight(
            [out] Int32* height);

        Recycle();

        IsRecycled(
            [out] Boolean* result);
    }
}
