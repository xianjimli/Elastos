
module
{
    interface ITableMaskFilter extends IMaskFilter {
    }

    interface ITableMaskFilterHelper {
        CreateClipTable(
            [in] Int32 min,
            [in] Int32 max,
            [out] ITableMaskFilter** tf);

        CreateGammaTable(
            [in] Float gamma,
            [out] ITableMaskFilter** tf);
    }
}
