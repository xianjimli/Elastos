
module
{
    interface ILayerRasterizer extends IRasterizer {
        AddLayer(
            [in] IPaint* paint,
            [in] Float dx,
            [in] Float dy);

        AddLayerEx(
            [in] IPaint* paint);
    }
}
