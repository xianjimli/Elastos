
module
{
    interface IDrawableContainer;

    interface ILevelListDrawable extends IDrawableContainer {
        AddLevel(
            [in] Int32 low,
            [in] Int32 high,
            [in] IDrawable* drawable);
    }
}
