
module
{
    interface ISyncStats {
        ToString(
            [out] String* str);
            
        /**
         * Reset all the counters to 0.
         */
        Clear();
        
        DescribeContents(
            [out] Int32* result);
    }
}