
module
{
    interface IDescribable;
    interface IComponentName;
    interface IBinder;

    interface IServiceConnectionInner extends IDescribable{
        Connected(
            [in] IComponentName* name,
            [in] IBinder* service);
    }
}
