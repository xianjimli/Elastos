
module
{
    interface IInputBindResult;

    interface IInputMethodClientStub extends IBinder {
        SetUsingInputMethod(
            [in] Boolean state);

        OnBindMethod(
            [in] IInputBindResult* res);

        OnUnbindMethod(
            [in] Int32 sequence);

        SetActive(
            [in] Boolean active);
    }
}
