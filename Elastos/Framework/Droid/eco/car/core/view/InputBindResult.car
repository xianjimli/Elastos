
module
{
    interface IInputMethodSession;

    interface IInputBindResult {
        GetIIMSession(
            [out] IInputMethodSession** session);

        GetId(
            [out] String* id);

        GetSequence(
            [out] Int32* sequence);
    }
}
