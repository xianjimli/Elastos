
module
{
    /**
     * Helper interface for IInputMethodStub to allow the input method to call back
     * to its client with results from incoming calls.
     * {@hide}
     */
    /* oneway */ interface IInputMethodCallbackStub extends IBinder {
        FinishedEvent(
            [in] Int32 seq,
            [in] Boolean handled);

        SessionCreated(
            [in] IInputMethodSessionStub* session);
    }
}
