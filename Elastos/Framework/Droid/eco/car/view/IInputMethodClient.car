
module
{
    interface IInputBindResult;

    interface IInputMethodClient {
        SetUsingInputMethod(
            [in] Boolean state);

        OnBindMethod(
            [in] IInputBindResult* res);

        OnUnbindMethod(
            [in] Int32 sequence);

        SetActive(
            [in] Boolean active);
    }
}
