
module
{
    interface IStatusBarIcon {
        Clone(
            [out] IStatusBarIcon** icon);

        IsVisible(
            [out] Boolean* isVisible);

        SetVisible(
            [in] Boolean visible);
    }
}

