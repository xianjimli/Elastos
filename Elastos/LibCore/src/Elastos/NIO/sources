TARGET_NAME= Elastos.NIO
TARGET_TYPE= eco

INCLUDESTEMP := $(INCLUDES);
INCLUDES := $(MAKEDIR)/../../../inc/Elastos/NIO;
INCLUDES += $(INCLUDESTEMP);

SOURCES = Elastos.NIO.car
SOURCES += CNotUsed.cpp

LIBRARIES = $(XDK_USER_LIB)/Elastos.Runtime.CarRuntime.lib
LIBRARIES += $(XDK_USER_LIB)/Elastos.Utility.lib
ifeq "$(XDK_TARGET_PLATFORM)" "win32"
LIBRARIES += $(XDK_LIB_PATH)/Elastos.Runtime.lib
else
LIBRARIES += $(XDK_TARGETS)/Elastos.Runtime.CarRuntime.eco
LIBRARIES += $(XDK_TARGETS)/Elastos.Utility.eco
LIBRARIES += $(XDK_USER_LIB)/crt0.lib
endif
