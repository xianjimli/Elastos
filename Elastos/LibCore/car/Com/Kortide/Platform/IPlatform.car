
module
{
    interface IFileSystem;
    interface INetworkSystem;

    interface IPlatform {
        GetFileSystem(
            [out] IFileSystem** fs);

        GetNetworkSystem(
            [out] INetworkSystem** ns);
    }
}