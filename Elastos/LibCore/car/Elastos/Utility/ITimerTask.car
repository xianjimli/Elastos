

module
{
    importlib("Elastos.Core.eco");

    interface ITimerTask extends IRunnable{
        Cancel(
            [out] Boolean* value);

        ScheduledExecutionTime(
            [out] Int64* value);
    }
}
