
module
{
    interface IChecksum;

    interface ICheckedInputStream extends IFilterInputStream {
        /**
         * Returns the checksum calculated on the stream read so far.
         *
         * @return the updated checksum.
         */
        GetChecksum(
            [out] IChecksum** checksum);
    }
}