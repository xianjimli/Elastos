
module
{
    interface ICookiePolicy;

    const CookiePolicy_ACCEPT_ALL = "accept_all";
    const CookiePolicy_ACCEPT_NONE = "accept_none";
    const CookiePolicy_ACCEPT_ORIGINAL_SERVER = "accept_original_server";

    interface ICookiePolicyHelper {
        GetCookiePolicy(
            [in] String type,
            [out] ICookiePolicy** policy);
    }
}