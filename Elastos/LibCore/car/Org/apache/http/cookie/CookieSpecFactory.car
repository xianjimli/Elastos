
module
{
    interface IHttpParams;
    interface ICookieSpec;

    /**
     *
     * @author <a href="mailto:oleg at ural.ru">Oleg Kalnichevski</a>
     *
     * @since 4.0
     */
    interface ICookieSpecFactory {
        NewInstance(
            [in] IHttpParams* params,
            [out] ICookieSpec** cookieSpec);
    }
}
