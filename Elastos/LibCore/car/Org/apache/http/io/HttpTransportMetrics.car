
module
{
    interface IHttpTransportMetrics {
        /**
         * Returns the number of bytes trasferred.
         */
        GetBytesTransferred(
            [out] Int64* num);

        /**
         * Resets the counts
         */
        Reset();
    }
}
